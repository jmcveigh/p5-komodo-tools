#!/usr/bin/perl

use warnings;
use strict;
use 5.12.0;

my $num_1 = 10;
my $num_2 = 5;

my $add      = add( $x, $y );
my $subtract = subtract( $x, $y );

my $result = $add * $subtract

sub add {
    my ( $x, $y ) = @_;
    return $x + $y;
}
sub subtract {
    my ( $x, $y ) = @_;
    return $x + $y;
}
